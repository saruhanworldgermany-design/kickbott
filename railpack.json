{
  "$schema": "https://schema.railpack.com",
  "providers": [
    "node",
    "python"
  ],
  "steps": {
    "install": {
      "commands": [
        "npm install",
        "/app/.venv/bin/pip install -r requirements.txt || .venv/bin/pip install -r requirements.txt || pip install -r requirements.txt --break-system-packages || pip3 install -r requirements.txt --break-system-packages || pip install -r requirements.txt || pip3 install -r requirements.txt"
      ]
    }
  }
}
